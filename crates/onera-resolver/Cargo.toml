[package]
name = "onera-resolver"
description = "Pure, deterministic dependency solver for Onera profiles"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# Deliberately minimal. This crate must stay pure: no sqlx, no reqwest, no
# tokio, no filesystem. If a dependency here needs I/O, the design is wrong.

[dependencies]
onera-core.workspace = true
serde.workspace = true

[dev-dependencies]
chrono.workspace = true
proptest.workspace = true
serde_json.workspace = true
