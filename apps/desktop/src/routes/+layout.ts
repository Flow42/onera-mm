// Tauri serves static files; there is no server-side rendering and no server
// data loading. Everything comes through the command bridge at runtime.
export const prerender = true;
export const ssr = false;
